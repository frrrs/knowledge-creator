/**
 * PostCSS 配置文件
 * 用于处理 CSS 的 PostCSS 插件配置
 * @type {import('postcss-load-config').Config}
 */
const config = {
  plugins: {
    // Tailwind CSS 插件 - 处理 Tailwind 指令和工具类
    tailwindcss: {},
  },
};

export default config;
