{
  "compilerOptions": {
    /* 类型库 */
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    /* 允许编译 JavaScript 文件 */
    "allowJs": true,
    /* 跳过声明文件的类型检查 */
    "skipLibCheck": true,
    /* 启用所有严格类型检查选项 */
    "strict": true,
    /* 不输出编译后的文件（Next.js 处理） */
    "noEmit": true,
    /* 启用 ES 模块互操作性 */
    "esModuleInterop": true,
    /* 使用 ES 模块 */
    "module": "esnext",
    /* 使用 bundler 模块解析策略 */
    "moduleResolution": "bundler",
    /* 允许导入 JSON 文件 */
    "resolveJsonModule": true,
    /* 确保文件可以安全地进行转译 */
    "isolatedModules": true,
    /* 保留 JSX 语法（由 Next.js 转换） */
    "jsx": "preserve",
    /* 启用增量编译 */
    "incremental": true,
    /* Next.js 插件 */
    "plugins": [
      {
        "name": "next"
      }
    ],
    /* 路径别名：@/* 映射到 ./src/* */
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
  },
  /* 包含的文件 */
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "dist/types/**/*.ts"
  ],
  /* 排除的文件 */
  "exclude": [
    "node_modules"
  ]
}
