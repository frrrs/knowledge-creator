{
  // 继承 Next.js 推荐的 ESLint 规则
  // next/core-web-vitals: 核心 Web Vitals 优化规则
  // next/typescript: TypeScript 支持规则
  "extends": ["next/core-web-vitals", "next/typescript"],
  // 自定义规则（如需添加）
  "rules": {}
}
